# Dependências locais (instaladas dentro do container)
node_modules

# Git e arquivos temporários
.git
.gitignore
.vscode/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos sensíveis e de ambiente
.env

# Pastas que serão persistidas via volumes (não devem ir pro build)
auth_info_baileys
data
temp
logs

# Arquivos de configuração e docs que não influenciam o build
Dockerfile
.dockerignore
*.md
*.txt
